# JavaTetris

## Install

Run the command :
```bash
git clone git@github.com:charlysgy/JavaTetris.git
```

Run the command :
```bash
./cmd.sh compile
```

Then, run the command : 
```bash
./cmd.sh run
```
